import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationStack {
            Text("Settings – coming later")
                .foregroundStyle(.secondary)
                .navigationTitle("Settings")
        }
    }
}
